# AI Agent Harness Implementation Audit

Date: 2026-05-20

Scope: `specs/architecture.md`, `specs/general_requirements.md`, and `specs/gils_feedback.md`.

This audit is intentionally conservative. `Done` means there is a concrete runtime path and at least basic verification in the repo. `Partial` means a usable slice exists but does not satisfy the full spec intent. `Missing` means no meaningful implementation was found.

| Area | Status | Evidence | Gap / notes |
| --- | --- | --- | --- |
| Provider and model config | Partial | `crates/agent-llm`, `crates/agent-config`, CLI/daemon/Tauri setup paths | OpenAI-compatible provider flow exists with configurable model/base URL behavior, first-class Ollama and llama.cpp aliases over local OpenAI-compatible endpoints, native Anthropic/Gemini provider wrappers across CLI/daemon/Tauri, native provider routing for model-backed ingestion guardrails, provider descriptor discovery across CLI/daemon/app surfaces, `metadata.provider_options` passthrough for provider-specific request parameters, typed CLI/remote flags, and app controls for common provider options (`top_p`, `top_k`, `reasoning_effort`). Provider descriptors now include typed option schemas for runtime fields and supported `provider_options`, the app consumes those schemas to enable/validate dynamic provider controls, model saves validate known provider option types/ranges, native-provider API base URLs are rejected, vision ingestion checks saved/provider/catalog-backed modality support with source details, and CLI/daemon/Tauri/app surfaces can probe declared capabilities, descriptor-backed native-provider capabilities, live HTTP OpenAI-compatible `/models` reachability/model presence, and native Anthropic/Gemini cloud catalog presence when API key env vars are configured. Saved model configs can also be exported/imported across CLI/daemon/Tauri/app and TUI. Live catalog probes now surface per-model reported modalities, capabilities, tool support, token/window limits, and pricing hints when native or OpenAI-compatible catalog fields expose them, including Gemini generation methods and token limits. Model probes also expose saved model-declared limits/pricing, load a versioned metadata catalog from `AGENT_MODEL_METADATA_CATALOG` or active-profile `models/metadata-catalog.json`, and fill missing live metadata from that catalog before the bundled default cloud-model fallback, so private/non-default models can carry offline capability hints without code changes. Provider descriptors can now be extended or overridden with a versioned `AGENT_MODEL_PROVIDER_CATALOG` file or active-profile `models/provider-catalog.json`, and CLI/daemon/Tauri/app provider lists plus saved-model validation/modality checks use those configured descriptors. Catalog-defined non-native provider ids now also feed the local CLI, remote CLI, daemon, and Tauri OpenAI-compatible runtime config, so saved models inherit descriptor API base URL, API-key env, and local/missing-key defaults at execution time; CLI `run`, remote `run`, and remote `run-start` accept `--provider-id <id>`, and the app provider selector renders configured descriptors instead of only hard-coded providers. The configured provider catalog and configured model metadata catalog can now be shown/exported/imported as portable JSON across CLI, remote daemon CLI, daemon HTTP, Tauri commands, app operations, and TUI `/models ...-catalog` commands; imported metadata catalogs normalize provider aliases and reject duplicate normalized provider/model entries. A shared model doctor report now checks saved models against active provider descriptors and configured/bundled metadata catalogs across CLI, remote CLI, daemon, Tauri, app, and TUI surfaces, and the app slash surface exposes `/models providers`, `/models doctor`, `/models provider-catalog`, `/models provider-catalog export <path>`, `/models provider-catalog import <path> --confirm`, `/models metadata-catalog`, `/models metadata-catalog export <path>`, and `/models metadata-catalog import <path> --confirm` for fast diagnostics and portable catalog transfer. The bundled metadata fallback now covers additional common non-default OpenAI and Gemini models such as `gpt-4o`, the `gpt-4.1` family, `gemini-2.5-pro`, and `gemini-2.5-flash-lite`, plus spec-named small routing aliases (`functiongemma`, `qwen3.5`, `gemma4`, `ministral`) for OpenAI-compatible, Ollama, and llama.cpp provider ids; broader provider plugin/update independence and exhaustive model-catalog coverage remain incomplete. |
| Tool registry, budgets, allowlists, visibility | Done | `crates/agent-core/src/lib.rs`, `crates/agent-tools/src/lib.rs` | Tool descriptors, categories, call budget, allowlists, global and per-tool/per-skill visibility levels, raw/interpreted output modes, tool-routing model policy, default and per-tool output interpreter model policy, approval gates, and permission audit data are present. The app can now set exact tool allowlists plus separate per-run and saved-agent tool and skill visibility levels over daemon or Tauri transport, can set per-run tool-routing and tool-output interpreter models, including via `/router-model <model>` and `/interpret <model>`, and can save/use advanced JSON-array per-tool and per-skill overrides without editing the saved agent file. |
| Subagents | Partial | `SubagentTool`, `ExecutionPolicy`, `ChildRunStarted`, `ChildRunCompleted` in `crates/agent-core/src/lib.rs` | Bounded subagent execution and trace linking exist. App run/save controls can now apply `max_subagent_depth` and `max_recursion_depth` through daemon or Tauri transport, matching the core depth/recursion enforcement policy. CLI/daemon/Tauri registry setup now advertises saved child-agent ids in the subagent tool schema, and `agent-core` uses the selected saved child `AgentConfig` when one is available instead of treating `agent_id` as a trace label only. Local, remote, daemon, Tauri, and app trace-tree views now render nested child-run topology from `ChildRunStarted`/`ChildRunCompleted` links so subagent branches can be inspected without manually correlating raw events. The app Run Tree labels root runs, child runs, external-agent links, event counts, link-event ids, and remote trace links instead of presenting remote external-agent children as missing traces, and tree nodes now let users move a run id into the shared Id field or load that node's trace/subtree directly when the trace is locally available; the Trace panel and `/trace <run-id>` shortcut can also load an explicit run id, while `/trace prompt` and `/trace clear` expose prompt recovery and trace cleanup without the buttons. The app tree now adds depth/leaf summaries, connector rails, and per-branch plus expand/collapse-all controls so nested subagent runs can be scanned without losing topology. The Trace panel and `/compare [primary-run-id] <compare-run-id>` shortcut can now load a secondary run id as a comparison trace and render primary-vs-compare deltas for trace volume, run-tree size/depth, context/model/tool usage, cost, duration, approvals, quality, and hook failures so alternative agent/subagent runs can be inspected side by side, while app `/compare clear` and `/replay [run-id] [--no-hooks] [--compare-source]` expose comparison cleanup, one-run hook overrides, and loaded-or-explicit trace prompt replay with optional source-vs-replay comparison from the keyboard. CLI `agent trace compare <run-id> <compare-run-id>`, remote CLI `agent remote trace-compare <run-id> <compare-run-id>`, and daemon `/trace/<run-id>/compare/<compare-run-id>` now expose the same side-by-side trace comparison artifact for scripted review, and CLI `agent trace replay <run-id> [--no-hooks] [--compare-source]` plus TUI `/replay [last|run-id] [--no-hooks] [--compare-source]` can run the original trace prompt again with the source agent id, an optional one-run hook override, and an immediate source-vs-replay comparison artifact. External-agent adapter tools now carry harness-native `agent`/`subagent` categories, so profile category grants and run category allowlists can address imported A2A/Hermes remote agents as subagent-style capabilities. Remote external-agent tool calls now also emit parent-trace `ChildRunStarted`/`ChildRunCompleted` links using `external-agent:<tool-id>` child labels, including failed-call completion status, while preserving the adapter's raw JSON output. Broader polished multi-run replay/comparison workflows remain limited. |
| Ingestion and provenance | Partial | `crates/agent-ingest`, `IngestionStarted`, `IngestionCompleted`, `IngestionReferenced` | Local ingestion, trace references, static findings, and model-backed guardrails exist across CLI/daemon/Tauri. Profile policy can provide a default guardrail model, saved model metadata can select OpenAI-compatible/Ollama/llama.cpp/Anthropic/Gemini provider settings for guardrail checks, completion traces surface finding summaries plus source snippets, `local-structured-v0` preserves headings/tables/image refs, and `local-layout-v0` adds PDF layout/OCR hooks via optional `pdftotext`/`tesseract`, image metadata fallbacks, built-in SVG title/description/text-label extraction for charts and diagrams, plus explicit model-backed vision enrichment for image and PDF ingestion through CLI/daemon/Tauri/app `vision_model` controls. The app can now set block/warn/allow ingestion-guardrail run policy, save/use the policy plus guardrail model on agent configs, and pass the active agent id to daemon/Tauri ingestion so per-agent guardrail-model defaults resolve for app-originated ingestion. Raster OCR now tries multiple local tesseract page-segmentation modes by default, can be pinned with `AGENT_INGEST_TESSERACT_LANG`, `AGENT_INGEST_TESSERACT_PSM`, `AGENT_INGEST_TESSERACT_OEM`, and `AGENT_INGEST_TESSERACT_EXTRA_ARGS`, and records the selected OCR mode in extracted text. Ingestion backend descriptors now expose a source compatibility matrix with extraction mode, optional local tool dependencies, and model modality requirements for PDF/image/SVG/text-style sources, and the app Ingestion panel renders those per-backend compatibility rows instead of hiding them behind raw JSON. Vision enrichment validates saved model, provider, and profile/bundled catalog modality metadata before calling the model, with source-aware requirements for image attachments and PDF document attachments, and the validation error reports the metadata source and advertised modalities. CLI/remote CLI/daemon/Tauri/app plus TUI `/ingest probe-vision` paths can now make an explicit live attachment smoke call against a configured model/source before full ingestion, surfacing provider acceptance/errors and token counts for image/PDF-style sources. Ingestion findings can be reviewed/approved/rejected across CLI/daemon/Tauri/app/TUI surfaces, approved high-risk findings no longer trip the blocking guardrail, and TUI `/ingest` can add/rerun/list/show/delete artifacts for inspection. Broader provider-native document/PDF compatibility verification across every provider remains incomplete. |
| Memory | Partial | `crates/agent-memory`, `MemoryRead`, `MemoryWritten`, context provenance | Local memory fragments, trace provenance, manual create/edit/delete/generate, export/import, rollback, profile-granted reads, topic-tagged memory creation/generation plus topic-filtered loading, and deterministic keyword-based topic/task classification on memory create/generate/import/edit exist across CLI/daemon/Tauri/app paths, with TUI create/generate/list/show/edit/delete/rollback/classify/export/import for local memory records. Manual generation can target a selected expanded conversation message range while preserving `source_conversation_id` and exclusive `messages:start..end` provenance across CLI, TUI, daemon, Tauri, and app controls. The daemon now exposes `POST /memory/generate-pending`, the remote CLI can trigger it with limit/target/topics, and an opt-in `AGENT_MEMORY_GENERATION_WORKER_INTERVAL_MS` background worker generates memories from new conversation message ranges with per-target checkpoints, batch limits, target selection, and worker topics. Conversation policy now includes `generate_memory` so pending/worker generation can be enabled or removed per conversation without changing the explicit one-off memory generation path, with CLI and app policy controls. Model-backed classification can now classify an existing memory through local CLI `memory classify`, TUI `/memory classify`, daemon `POST /memory/classify`, or Tauri `memory_classify` using saved model runtime settings (or `AGENT_MEMORY_CLASSIFICATION_MODEL`), persist normalized model topics/tasks, and record the model in memory trace provenance; local/remote CLI can pass `--agent` so classification falls back to an agent/profile/global `memory_model`, TUI uses the active agent id when `--agent` is omitted, daemon/app/Tauri classify paths can pass the active agent id, and explicit per-call model overrides still win. Agent/global/profile config can now persist the selected memory backend, CLI and remote agent-save flows expose `--memory-backend`, the app run/save controls can apply `memory_backend` plus `memory_model` through daemon or Tauri transport, and `MemoryRead` trace events record the resolved backend id. The built-in backend set now includes `local-markdown-v0` and a real `local-jsonl-v0` store with generation, rollback, import/export, descriptor discovery, and context loading across CLI/daemon/Tauri setup paths; profile-granted memory reads scan supported local backends in the source profile, and CLI/remote CLI/daemon/Tauri/app/TUI `memory access` reports show the active profile's local plus profile-granted records with source profile, backend, grant id, and topic filtering. Memory create/generate/import paths can now persist an explicit owning agent across CLI/remote daemon/Tauri/app flows, and conversation/pending generation defaults ownership to the source conversation agent so profile memory grants by agent id can match real records. The app slash surface now covers `/memory create <content>`, `/memory generate <text>`, `/memory generate-conversation [id] <from>:<to>`, `/memory edit <id> <content>`, `/memory delete <id> --confirm`, and `/memory rollback --confirm` over the existing Memory panel operations, so manual and conversation-range memory workflows no longer require raw panel buttons. Richer cross-profile/team workflows and external memory SDK/framework adapters remain incomplete. |
| Context preview and config provenance | Done | `ContextSnapshot`, `preview_context`, frontend trace/context panels | Context snapshots include visible tools, skills, memory, artifacts, estimates, and provenance records. App `/usage trace <run-id>` and `/usage run <run-id>` shortcuts can load persisted run trace usage totals without first making that run the latest run. |
| Prompt refinement | Done | `PromptRefinement`, `PromptRefinementStarted`/`PromptRefinementCompleted`, `AgentPromptRefinementConfig`, CLI/daemon/Tauri/app refine controls | Per-agent prompt preprocessing exists with optional model override, multiple id/when refinement rules, trace preservation of original and refined prompts, and optional agent awareness by injecting refinement guidance into the system prompt. CLI local/remote run flags, agent-save config, daemon/Tauri run options, TUI trace rendering, and app `/refine` controls expose the runtime path. The app saved-agent flow now persists simple prompt-refinement controls and reapplies saved prompt-refinement settings when a saved agent is used from the Agents operations or `/agents use`. |
| Compaction | Partial | `crates/agent-compaction`, `ContextPolicy::compaction`, compacted context fields plus `compaction_review` in `ContextSnapshot`, CLI run/preview/agent-save flags, daemon/Tauri/app `RunOptions` controls, conversation `policy`, `compact keep/keep-run/export/import`, remote `compact keep-run`, `/compactions` list/show/delete/export/import/keep routes, app Keep Compact, `/compact keep`/`/compact keep-run [run-id]`/`/compactions keep`/`/compactions keep-run [run-id]`, and Compactions Ops group, TUI `/compact keep/list/show/delete/export/import` | Manual compaction support exists, and agent/global/profile policy can now set `max_tokens_before_compaction`, `max_compaction_output_tokens`, and `compaction_guidance`. CLI local/remote run and preview workflows, CLI agent-save workflows, daemon/Tauri runtime options, and the app Context panel can override those auto-compaction settings. Conversation documents now carry explicit compaction policy overrides that are applied after resolved agent/profile policy and before per-run overrides, with CLI `conversation policy`, daemon/Tauri policy endpoints, and app selected-conversation save/clear controls. When conversation history exceeds the configured threshold, preview/run context auto-compacts older history into a `<auto-compaction>` block while keeping the latest user prompt explicit and leaving the stored full conversation available for branches. Users can explicitly keep an already compacted context as a portable `CompactionRecord`, linked to a conversation id when supplied so recovery plans can discover it, and can now list, show, delete, export, and import portable compaction JSON artifacts across CLI/daemon/Tauri/app surfaces, with TUI list/show/delete/export/import coverage for portable records. The app prompts after successful auto-compacted runs to keep the generated context, shows rough before/after token savings in context review, and renders a before/after compaction review from the same `ContextSnapshot` contract used by execution. Headless local and remote CLI runs now print `compact keep-run` hints when auto-compaction occurs, and the TUI prompts for `/compact keep` after completed auto-compacted runs. |
| Branching and deletion | Partial | `crates/agent-conversations`, CLI/daemon/Tauri conversation commands, frontend Conversations panel, TUI `/conversation` slash commands, runtime `conversation_id` options | Conversation branching, branch-tree listing, expanded conversation review, deletion side-effect previews, leaf message-range deletion, and explicit recovery plans from linked compactions/memories exist across CLI/daemon/Tauri/app surfaces. CLI local/remote runs, daemon/Tauri runs/previews, and the app Context panel can load a selected conversation branch into the next context by id, so branch recovery can use the full saved conversation before compaction policy is applied. Conversation branches can now persist memory-loading, memory-generation, tool-category, skill-category, and compaction policy overrides, with new branches copying parent policy at branch creation so a branch keeps the parent run posture unless explicitly changed; local and remote CLI plus app policy controls can set conversation category allowlists that are applied before per-run category overrides. Remote CLI conversation commands can list/tree/show/recover, set policy, review delete plans, delete branches, delete leaf ranges, and delete all branches owned by an agent id through daemon endpoints; the shared conversation tree now exposes branch-point markers and first-message topic previews, the app Conversations panel can plan/delete all branches for the active Agent id, shows branch-tree totals/roots/branch-points/leaves/depth, highlights the selected branch, and renders dedicated topology rows with topic/reason labels instead of generic operation cards. Daemon/Tauri delete results now remove/report linked compactions and memories for deleted branches. The TUI can review recovery plans, branch trees, deletion side-effect plans, policy overrides, and message ranges with `/conversation recover <id>`, `/conversation tree`, `/conversation browse`, `/conversation policy [<id>]`, `/conversation delete-plan [<id>] [--recursive]`, and `/conversation range [<id>] <from>:<to>`. It now renders the branch tree as a numbered picker and as a keyboard-native branch browser with branch points, topic previews, Up/Down navigation, Enter selection, recovery-plan shortcut, delete-plan shortcut, and Esc close. It supports selecting by id or tree number, lets selected ids be omitted for policy/range/delete-plan commands, stages conversation deletes or range deletes, and requires `/conversation confirm` or `/conversation cancel` before destructive changes execute. Recovery output includes a concise guidance summary before the raw plan JSON. The app slash surface now covers `/conversation list|tree|select|show|recover|policy show|policy apply|policy save|policy clear|delete-plan|delete --confirm|range-delete --confirm`, reusing the Conversations panel operations and selected-id fallback for review, recovery, and policy workflows. |
| Stop, resume, and mid-run guidance | Partial | `RunPaused`, `RunCancelled`, `GuidanceInjected`, `build_resume_plan`, remote `run-start`/`resume-start`/`run-wait`/`run-status`/`run-events` | Stop/guidance/resume primitives exist. Remote CLI async runs and resumed runs can now be started, polled to a terminal status, bounded by an optional timeout, and optionally reported with newly observed cursor-based events. The app daemon resume path now uses `/resume/start` and the same live trace/status polling as fresh runs, so resumed runs keep Stop/Guide targeting the active resumed run instead of blocking on `/resume`. The app Guide control and `/guide` shortcut now require the currently running root run id instead of a stale previous `lastRunId`, while the Stop control keeps the returned stopped-run summary compaction in the Compactions list when Summarise mode is used, matching the daemon/Tauri cancel response instead of dropping the retained context id. Agent/global/profile config can now resolve a default stopped-run retention mode (`discard` or `summarise`), local and remote `agent save` expose `--stop-retention-mode`, the app Stop mode can remain on Default so daemon/Tauri transports omit the explicit mode, and TUI/daemon/Tauri stop paths use the resolved default when no explicit mode is supplied while preserving explicit runtime overrides. The app `/stop [--summarise|--discard|default] [reason]` shortcut can stop active runs with an optional retained-summary mode and human reason while still using the same words to set the default stop mode when idle. Local and remote CLI `resume-plan`, TUI `/resume plan [last|run-id] [--from-event N]`, and app `/resume plan [run-id] [--from-event N]` or `/resume-plan [run-id] [--from-event N]` preview the generated resume prompt without starting work, and local/remote/TUI/app `/resume <run-id> [--from-event N]` exposes selected-run/selected-step resume across daemon and Tauri transports. The TUI accepts `/guide <text>` and `/stop [--summarise] [reason]` while a run is active, in addition to the existing stop keys, and `/stop --summarise` retains a portable stopped-run compaction summary for later resume context. The TUI can also start `/resume` from the last or a specified persisted run with optional `--from-event`, reusing retained stopped-run compactions and streaming resumed events through the live TUI trace. Broader cross-surface UX still needs hardening. |
| Cost and quality | Done | `CostPolicy`, trace summaries, `QualityScored`, CLI/Tauri quality controls | Token/cost accounting and user quality scoring are represented in traces and UI. Quality score records can be queried by run with bookmarkable run/event ids across local CLI, daemon/remote CLI, and TUI paths. App saved-agent save/use controls now round-trip per-agent input/output cost rates, and saved-agent cards surface configured rates. App and TUI score shortcuts can target the last answer, full conversation, or a labelled selected range while preserving the target in the `QualityScored` trace event. |
| Agent-created capabilities | Partial | `crates/agent-capabilities`, review/quarantine CLI/Tauri surfaces | Capability quarantine and review paths exist. Reviewed tool drafts promote through the adapter registry as MCP adapter packages, and unsafe agent-created tool drafts are staged before promotion so static-scan failures do not leave partial adapter-pack source files behind. Capability drafts are portable across CLI/daemon/Tauri/app export/import paths plus TUI list/show/export/import/delete commands, with imported drafts forced back into quarantine for review/rollback safety. Capability-draft tool access now resolves from global/profile/agent policy plus conversation overrides, remains disabled by default, can be configured through local/remote `agent save`, can be forced on for direct/explicit runs, and can carry policy guidance into the tool descriptor across CLI/daemon/Tauri registries. The capability draft schema, CLI help, and app selector now advertise `subagent` as an explicit alias for agent-config drafts, portable imports normalize legacy `subagent` draft kinds back to stored `agent` drafts, and tests cover the alias/schema path. A shared capability draft doctor report now summarizes draft counts, quarantine/allow/reject state, review-needed warnings, and promotion targets for adapter-package, skill-doc, and agent-config candidates across local CLI, remote daemon CLI, daemon HTTP, Tauri commands, app `/capabilities doctor`, and TUI `/capabilities doctor`; tests cover both the shared report counts and daemon route precedence over dynamic draft ids. TUI capability allow/reject commands require `--confirm` and reuse the same promotion/quarantine logic as the headless CLI; the headless review path now has direct coverage proving an allowed MCP tool draft becomes a registry-visible adapter tool with draft provenance and an imported `subagent` alias draft becomes a saved agent config. Agent-created MCP tool drafts now reject ambiguous command-plus-URL runtimes plus malformed args/env/header declarations before promotion so transport selection is explicit and proposed runtime arguments or secret maps cannot be silently dropped. `agent-tools` coverage now proves a promoted agent-created MCP package is converted into runtime MCP specs with args/env/headers preserved and registered as approval-gated tools with draft provenance. Broader adapter-pack integration still needs more coverage. |
| Profiles, grants, and policy | Partial | `crates/agent-config`, `crates/agent-secrets`, `crates/agent-sandbox`, `crates/agent-bundles` | Profiles, grants, secrets, sandbox classification, and approvals exist. Profile CRUD and cross-profile grant management are exposed in local CLI/TUI plus daemon HTTP, Tauri commands, and the app Profiles panel, with explicit grant kind/resource records preserved; app slash shortcuts now cover profile show/create/delete/grants/grant/revoke flows without requiring the raw operation buttons. Deleting a profile now removes incoming grants from other profiles while preserving unrelated grants, so stale grants to deleted profiles are not left behind, and deleting the active profile is rejected in the resolver and guarded in the app. Approval decisions/execution can be protected by an optional SHA-256 unlock secret and optional HMAC-SHA256 approval signature across CLI/daemon/Tauri approval paths. Agent/global/profile config and app run/saved-agent controls can declare a delegated approval controller plus allowed tool/category scope; approval requests advertise the scoped controller and CLI/daemon/Tauri/app decisions can record a matching delegated controller. Delegated-controller verification produces a structured trace-local assessment (`scope_verified`, controller id, advertised scope, and reason), and CLI/remote CLI/daemon/Tauri plus TUI `/approval assess` paths can now run the configured controller agent's model without tools, parse an approve/reject/needs-human recommendation, and append an `ApprovalControllerAssessed` trace event that CLI/TUI/daemon/Tauri approval and trace views surface. The app Approvals panel can now call the same daemon/Tauri approval assessment path, keep the assessment on the approval card, and render assessment trace events in the timeline. TUI `/approval` can also list approvals, approve/reject requests, and execute approved tool calls against the same trace verification helpers. Profile/config/cache bundles preserve the active profile id in their manifest and reject unsafe symlink-style tar entries during import. Enforcement is stronger for harness tools than for all future external adapters, and broader autonomous approval workflows across every surface remain incomplete. |
| Security, sandboxing, and secrets | Partial | `agent-sandbox`, `agent-secrets`, redaction in `agent-core`, shell/code sandbox wrapper support in `agent-tools` | Secret redaction, permission classification, and hardened third-party static-scan markers exist. Secret backend inspection and redacted secret metadata lifecycle operations (list/show/store/rotate/delete) are now exposed through daemon HTTP, Tauri commands, and the app Profiles panel without returning secret values. Shell and code execution tools can run through configured non-shell sandbox wrappers (`AGENT_SHELL_SANDBOX_COMMAND`/`AGENT_CODE_SANDBOX_COMMAND` plus JSON args/label env vars), can be made fail-closed with `AGENT_SHELL_SANDBOX_REQUIRED`/`AGENT_CODE_SANDBOX_REQUIRED`, and still keep approval gates plus minimal inherited environment. When `AGENT_SHELL_SANDBOX_AUTO` or `AGENT_CODE_SANDBOX_AUTO` is enabled and no explicit wrapper is configured, the matching runner auto-detects macOS `sandbox-exec` or Linux `bwrap` wrappers and contextualizes cwd/temp placeholders before launching the shell or language runner. Approval unlock secrets are compared by hash, approval signatures bind to `run_id:approval_id`, delegated controller approvals are scope-checked against the original approval request, and neither approval secret is recorded in run traces. Sandbox reports now emit OS-specific profile plans/templates for macOS `sandbox-exec`, Linux bubblewrap, and Windows restricted-token/Job Object/WFP isolation when permissions are requested. Tool-call permission audits now also include connector isolation metadata for MCP/A2A/HTTP external-agent and adapter-provenanced tools, recording normalized connector kind, provenance, credential boundary, and applied harness policy boundaries beside the sandbox report. Broader connector isolation remains incomplete. |
| Batch/list mode | Done | `BatchRunStarted`, `BatchItemStatus`, `BatchRunCompleted`, CLI batch flow | Batch traces and headless execution paths are implemented. |
| Artifact generation and viewing | Done | `ArtifactTool`, `cache/artifacts`, CLI/daemon/Tauri/TUI artifact list/show/open/delete | Scoped generation for text, markdown, CSV, JSON, HTML, PDF, DOCX, XLSX, and PPTX exists with list/show/open/delete surfaces, so generated cache artifacts can be reviewed and removed from CLI/daemon/Tauri/TUI/app paths. The app can also load scoped artifact data URLs and preview text, markdown, CSV, JSON, HTML, PDF, and audio artifacts inline, while retaining one-click OS open for formats that need the default app. |
| Storage management | Partial | `agent-storage`, CLI/daemon/Tauri/app/TUI storage report, deletion surfaces | Storage reports break down config/profile/cache/state footprint, identify largest files, and can now compare total footprint against an `AGENT_STORAGE_QUOTA_BYTES` limit across CLI/daemon/Tauri/app JSON and UI output, with TUI `/storage report` for local inspection. The app slash surface now covers `/storage report` and `/storage prune-cache <days> [--apply]`, so cache-retention dry runs and confirmed cleanup can be triggered without the raw storage panel. Users can manually reduce footprint through conversation, memory, compaction, ingestion, generated artifact, and bundle cleanup/export paths. Cache retention now has a dry-run plan and explicit apply path across local CLI, TUI `/storage prune-cache`, remote daemon CLI, daemon HTTP, Tauri commands, and app controls, deleting only cache files older than a user-supplied retention window. The daemon can also run an opt-in cache retention worker with `AGENT_STORAGE_RETENTION_WORKER_INTERVAL_MS` and `AGENT_STORAGE_CACHE_RETENTION_DAYS`. `AGENT_STORAGE_QUOTA_BYTES` is now enforced for generated artifact cache writes that know their byte size before write, saved prompts, skill registry writes, ingestion artifacts, compaction records, conversation documents, memory writes with planned backup rotation, batch plans, config/profile/model/grant saves, capability drafts created through the env-backed store, adapter registry manifests, agent-created adapter source staging, bundle import entries, bridge delivery dead letters, file-backed secret metadata/value writes, keychain secret metadata writes, and memory-generation checkpoints; local voice commands that write their own target artifact are rolled back if the post-write quota check fails. `agent-storage` now also exposes a shared quota-checked file writer and the config save helper, adapter registry, prompt store, skill registry, compaction store, conversation store, ingestion store, batch plan store, env-backed capability draft store, env-backed secret stores, daemon bridge-delivery store, and memory-generation checkpoint writer use it for internal config/manifest/source/document/artifact/metadata/checkpoint writes, reducing quota bypass risk for config/adapter/prompt/skill/compaction/conversation/ingestion/capability/secret/daemon-adjacent code. Broader hard quota enforcement for future external adapter writers remains incomplete. |
| Export/import portability | Partial | `agent-bundles`, `agent-config`, CLI/daemon/Tauri/app import-export paths | Profile/config/cache bundles, saved agents, model configs, provider catalogs, model metadata catalogs, skills, capability drafts, adapter manifests, prompts, compactions, memories, and generated artifacts have concrete portable paths across CLI/daemon surfaces where applicable. The app can now save the current setup as a saved agent config and list/show/select/export/import/delete active-profile saved agent configs over daemon or Tauri transport; explicitly profile-granted saved agents are also listable, showable, selectable, and exportable through the same agent registry surfaces, with source-profile/grant provenance and delete actions guarded in the app and core delete path so shared agents are removed by revoking grants rather than deleting from the target profile. The Profiles panel can export/import the active profile/config/cache bundle with active-profile-aware default filenames, and app `/bundles backup`, `/bundles export <path>`, and `/bundles import <path> --confirm` shortcuts expose those bundle paths without raw panel buttons, in addition to existing model, provider-catalog, metadata-catalog, bundle, skill, memory, compaction, capability, adapter, and artifact operations. Portable adapter manifests now preserve redacted MCP/A2A runtime launch or connection metadata (`command`, `url`, `transport`, input/output modes, env key names, header key names, and auth scheme names) while still omitting credential values. Connector-specific credentials still require manual reconfiguration after import. |
| Daemon/API/Tauri/TUI accessibility | Partial | `crates/agent-daemon`, `crates/agent-tauri`, `crates/agent-cli/src/tui.rs` | Main controls are exposed across surfaces. Daemon `/profiles` and `/profile-grants` routes plus Tauri `profile_*` commands now cover current/list/show/create/delete and grant/list/revoke operations, and daemon `/secrets` routes plus Tauri `secret_*` commands cover backend/list/show/store/rotate/delete operations with redacted responses. The app Profiles panel plus app `/profiles`, `/profiles current`, `/profiles grants`, `/secrets backends`, `/secrets list`, `/secrets show <id>`, and `/secrets delete <id>` shortcuts can use either transport; app `/agents`, `/agents show <id>`, `/agents use <id>`, and `/agents delete <id>` shortcuts cover saved-agent inspection/selection/deletion without relying on raw buttons. The app prompt slash surface now covers `/prompts`, `/prompts list`, `/prompts show <name>`, `/prompts use <name>`, `/prompts preview <name>`, and `/prompts delete <name>` alongside `/run <prompt-name>` and `/prompt <prompt-name>`. The app model slash surface now covers `/models`, `/models list`, `/models show <id>`, `/models probe <id>`, `/models delete <id>`, `/models providers`, `/models doctor`, `/models provider-catalog`, `/models provider-catalog export <path>`, `/models provider-catalog import <path> --confirm`, `/models metadata-catalog`, `/models metadata-catalog export <path>`, and `/models metadata-catalog import <path> --confirm`; app `/memory`, `/memory list`, `/memory access`, `/memory backends`, `/memory preview`, `/memory create <content>`, `/memory generate <text>`, `/memory generate-conversation [id] <from>:<to>`, `/memory classify <id>`, `/memory edit <id> <content>`, `/memory delete <id> --confirm`, and `/memory rollback --confirm` shortcuts cover memory inspection, manual generation, conversation-range generation, editing, and safe destructive cleanup while preserving `/memory on|off|status` context toggles; app `/compactions`, `/compactions list`, `/compactions keep`, `/compactions keep-run [run-id]`, `/compactions show <id>`, `/compactions use <id>`, `/compactions export <id> [path]`, `/compactions import <path>`, and `/compactions delete <id>` shortcuts cover keeping current or run-derived auto-compacted context plus portable compacted-context artifact inspection, reuse, transfer, and confirmed deletion; app `/ingest`, `/ingest list`, `/ingest backends`, `/ingest add <path> [--backend <backend>] [--vision-model <model>] [--guardrail-model <model>]`, `/ingest probe-vision <path> --model <model>`, `/ingest show <id>`, `/ingest rerun <id> [--backend <backend>] [--vision-model <model>] [--guardrail-model <model>]`, `/ingest use <id>`, `/ingest preview <id>`, `/ingest review <id> <finding> <decision>`, and `/ingest delete <id>` shortcuts cover ingestion artifact listing, backend discovery, creation, inspection, reruns, context inclusion, preview, finding review, and safe deletion; app `/artifacts`, `/artifacts list`, `/artifacts show <id>`, `/artifacts open <id>`, `/artifacts preview <id>`, and `/artifacts delete <id>` shortcuts cover generated-artifact inspection, inline preview, OS open, and safe deletion; app `/voice status`, `/voice capture`, `/voice stop`, `/voice transcribe [path]`, `/voice speak [text]`, and `/voice stage [text]` shortcuts cover voice capture, transcription, direct speech output, and staging without the raw voice buttons. App `/python <code>`, `/typescript <code>`, and `/ts <code>` shortcuts invoke the approval-gated native code execution tools, and app `/x402 request <url>`/`/payment x402-request <url>`, `/x402 required ...`/`/payment x402-required ...`, and `/x402 settle ...`/`/payment x402-settle ...` shortcuts invoke the approval-gated x402 payment request, challenge, and settlement tools with method, spend-limit, autopay, secret-handle, payment-requirement, and facilitator flags, without requiring raw tool JSON. TUI `/agent <id>` switches the active saved agent in-session, rebuilding the agent-scoped registry so subsequent runs, saved prompts, memory classification, subagent choices, voice tools, and hooks use the selected agent. The app Trace panel now loads the same nested child-run trace tree exposed by CLI/daemon/Tauri APIs, reducing raw-event-only inspection for subagent branches. The app/Tauri prompt library can now use the current Agent id as an agent-scoped saved-prompt library, with run/use fallback to global prompts, and TUI `/run <prompt-name>` uses the same active-agent prompt fallback. TUI `/prompts` slash commands can list/show/save/delete global or agent-scoped saved prompts. TUI `/adapters` slash commands can list/show adapter manifests, inspect adapter doctor output, install OpenClaw adapters as quarantined skills, import/export portable adapter manifests, quarantine adapters, and require explicit `--confirm` before allowing one; app `/adapters`, `/adapters list`, `/adapters doctor`, `/adapters show <id>`, `/adapters import <path>`, `/adapters import-manifest <path>`, `/adapters export <id> <path>`, `/adapters install-skill <id>`, `/adapters allow <id> --confirm`, and `/adapters quarantine <id>` shortcuts cover the same adapter review, transfer, install, and quarantine actions over daemon/Tauri transport. The app Adapters/Storage panel plus app `/bridge-deliveries`, `/bridge-deliveries retry <id>`, and `/bridge-deliveries retry-all` shortcuts can list and retry daemon bridge-delivery dead letters over daemon transport. TUI `/models` slash commands can list/show/export/import saved model configs, inspect configured providers, run the model doctor report, and show/import/export portable provider and metadata catalogs. TUI `/agents` slash commands can list/show/export/import saved agent configs and require confirmation before deleting one. TUI `/profiles` slash commands can inspect/create/delete local profiles and manage cross-profile grants with confirmation before destructive changes. TUI `/secrets` slash commands can inspect backend/record metadata, store or rotate redacted secret handles, and require confirmation before deleting one. TUI `/bundles` slash commands can export profile/cache bundles and require confirmation before importing one, and app `/bundles backup`, `/bundles export <path>`, and `/bundles import <path> --confirm` shortcuts export or import the active profile bundle. TUI `/skills` slash commands can list/show/import/export quarantined skills and confirm allow/quarantine review decisions; app `/skills` slash commands now cover list/show/import-openclaw/import-doc/export plus confirmed allow/quarantine review actions. TUI `/compact` slash commands can keep auto-compactions and list/show/delete/export/import portable compaction records. TUI `/memory` slash commands can create/generate/list/access/show/edit/delete/rollback/classify memory records, inspect memory backends, and export/import agent or user memory files. TUI `/storage` slash commands can report local storage and preview/apply cache retention pruning. TUI `/capabilities` slash commands can list/doctor/show/export/import quarantined capability drafts, confirm allow/reject review decisions, and confirm delete decisions; app `/capabilities` slash commands now list/doctor/show/export/import/delete drafts and require `--confirm` before allow/reject review actions. TUI `/artifacts` slash commands can list/show/open generated artifacts and require confirmation before deleting one. TUI `/ingest` slash commands can add/rerun/probe/list/show/delete ingestion artifacts and review ingestion findings. TUI `/approval` slash commands can list, assess, approve/reject, and execute local trace approvals, including while a run is active; app `/approval list|assess|approve|reject|execute` shortcuts cover current, last, or explicit run ids over daemon/Tauri transport. TUI `/trace [last|run-id]`, `/trace summary [last|run-id]`, `/trace tree [last|run-id]`, `/compare [primary-run-id] <compare-run-id>`, and `/replay [last|run-id] [--no-hooks] [--compare-source]` shortcuts now inspect local durable trace summaries, child-run trees, side-by-side trace comparison rows, source-vs-replay comparisons, and stream fresh trace-prompt replays from the transcript. The app `/compare [primary-run-id] <compare-run-id>` shortcut can load primary and comparison traces in one command, and app `/replay [run-id] [--no-hooks] [--compare-source]` plus the Trace panel Replay Compare button can rerun a loaded or explicit trace prompt and auto-load source/replay comparison rows. Some newer features have operational panels rather than fully polished UX. |
| App saved-agent shortcuts | Done | `crates/agent-tauri/frontend/src/App.tsx` | The app slash surface now includes `/agents save <id> <system prompt>`, `/agents export <id> <path>`, and `/agents import <path>` alongside list/show/use/delete, so saved-agent create and portability flows are reachable without raw operation buttons. |
| App saved-model shortcuts | Done | `crates/agent-tauri/frontend/src/App.tsx` | The app slash surface now includes `/models save <id> [json]`, `/models save-current`, `/models export <id> <path>`, and `/models import <path>` alongside list/show/probe/delete and catalog commands, so saved model metadata and portability flows are reachable without raw operation buttons. |
| App capability propose shortcut | Done | `crates/agent-tauri/frontend/src/App.tsx` | The app slash surface now includes `/capabilities propose <tool|skill|agent|subagent> <name> <body>`, so users can create quarantined capability drafts from the keyboard before reviewing, exporting, allowing, or rejecting them. |
| TUI capability propose shortcut | Done | `crates/agent-cli/src/tui.rs` | The TUI `/capabilities` slash surface now includes `/capabilities propose <tool|skill|agent|subagent> <name> <body>`, creating quarantined drafts through the shared `CapabilityDraftStore` without leaving the terminal UI. |
| Remote skill inspect shortcut | Done | `crates/agent-cli/src/main.rs`, `crates/agent-cli/src/headless.rs` | The remote CLI now includes `agent remote skill inspect <id>` and `agent remote skill show <id>`, calling daemon `GET /skills/<id>` so daemon-host quarantined or allowed skill docs can be reviewed before allow/quarantine decisions. |
| App skill inspect shortcut discoverability | Done | `crates/agent-tauri/frontend/src/App.tsx` | The app slash catalog and `/skills help` now advertise `/skills inspect <id>` alongside `/skills show <id>`, matching the first-command skill inspection shape already accepted by the parser. |
| TUI skill inspect shortcut discoverability | Done | `crates/agent-cli/src/tui.rs` | The TUI `/skills help` text now advertises `/skills inspect <id>` alongside `/skills show <id>`, matching the first-command skill inspection shape already accepted by the parser. |
| TUI saved-model shortcuts | Done | `crates/agent-cli/src/tui.rs` | The TUI `/models` slash surface now includes `/models save <id> [json]` and confirmed `/models delete <id> --confirm` alongside list/show/export/import, provider, doctor, and catalog commands, so terminal users can create and remove saved model metadata without leaving the TUI or switching to the headless CLI. |
| TUI model probe shortcut | Done | `crates/agent-cli/src/tui.rs` | The TUI `/models` slash surface now includes `/models probe <id>`, reusing the shared `ConfigResolver::probe_model_capabilities` path and printing the same structured probe artifact available from CLI, daemon, Tauri, and app surfaces. |
| TUI model catalog import confirmations | Done | `crates/agent-cli/src/tui.rs` | TUI `/models provider-catalog import <path>` and `/models metadata-catalog import <path>` now stage a pending confirmation and require `--confirm`, matching the app shortcuts before overwriting active-profile catalog state. |
| TUI saved-agent shortcuts | Done | `crates/agent-cli/src/tui.rs` | The TUI `/agents` slash surface now includes `/agents save <id> <system prompt>` and `/agents use <id>` alongside list/show/export/import/delete, so terminal users can create a minimal saved agent config and switch to one from the same command family without leaving the TUI or switching to the headless CLI. |
| TUI adapter source inspect/import shortcuts | Done | `crates/agent-cli/src/tui.rs` | The TUI `/adapters` slash surface now includes `/adapters inspect <path>` and `/adapters import <path>` in addition to `/adapters import-manifest <path>`, so local adapter packages can be reviewed as normalized manifests and stored in quarantine from the TUI like they can from the app and headless CLI. |
| TUI ClawHub catalog shortcuts | Done | `crates/agent-cli/src/tui.rs` | The TUI `/adapters clawhub` slash surface now supports `search`, `inspect`, `pin`, and quarantined `install` against a local ClawHub catalog, matching the headless/remote CLI catalog workflow for reviewable adapter discovery. |
| App adapter source inspect shortcut | Done | `crates/agent-daemon/src/main.rs`, `crates/agent-tauri/frontend/src/App.tsx` | The app `/adapters inspect <path>` shortcut now reviews a local adapter source as a normalized manifest without importing it, and daemon transport exposes `POST /adapters/inspect` for the same non-persisting review path as Tauri. |
| Remote adapter source inspect shortcut | Done | `crates/agent-cli/src/main.rs`, `crates/agent-cli/src/headless.rs` | The remote CLI now includes `agent remote adapter inspect <path>`, posting to daemon `POST /adapters/inspect` so daemon-host adapter sources can be reviewed as normalized manifests before quarantine/import. |
| App ClawHub catalog shortcuts | Done | `crates/agent-tauri/src/lib.rs`, `crates/agent-tauri/frontend/src/App.tsx` | The Tauri/app adapter surface now exposes `/adapters clawhub search <catalog> [query]`, `inspect`, `pin`, and quarantined `install` over both daemon and in-process transports, reusing the same ClawHub provider workflow as the CLI. |
| Adapter compatibility | Partial | `crates/agent-adapters`, general requirement adapter mapping | OpenClaw/ClawHub/MCP/Hermes/A2A concepts are represented. MCP, Hermes, and A2A imports now surface declared env/auth/secret names as reviewable secret requirements without persisting secret values. A2A Agent Cards are normalized into quarantined external-agent capabilities using declared skills, endpoint/interface metadata, security schemes, and redacted per-capability runtime metadata, including API-key security scheme header names as header key hints without credential values; MCP imports similarly preserve command/HTTP transport details plus env/header key names for review/export without saving env or header values. Allowed HTTP MCP tools forward manifest headers on initialize/tools/call and resolve `secret://...` header values through the secret store. Hermes plugin `external_agents`/`subagents` declarations now preserve endpoint, A2A transport, input/output modes, auth scheme hints, and header key hints when supplied; allowed external-agent tools surface those header key hints without storing credential values, resolve per-call header values supplied as `secret://...` handles, and auto-fill missing declared header keys from same-named active secret records before HTTP/A2A calls. The CLI/app adapter review paths display those requirements and runtime hints before allow/quarantine decisions. Static scanning now also flags common package-install commands, auto-update/self-update markers, browser credential stores, and wallet seed phrases before allow decisions. OpenClaw/AgentSkills adapter packages can now be installed into the native skill registry as quarantined `SkillDoc` records with adapter package/digest provenance across CLI, remote daemon CLI, daemon HTTP, Tauri commands, app controls, and TUI `/adapters install-skill`; imported and portable skill docs preserve prompt-injection scan findings while staying quarantined, and high-risk findings block activation at the explicit allow step. Allowed A2A external-agent capabilities with HTTP endpoints, including explicitly A2A-compatible Hermes external-agent declarations, now register as approval-gated `external-agent`/`a2a` tools across CLI, daemon, and Tauri registry setup, including profile grants by tool or category, and execute JSON-RPC `message/send` calls with direct user input. Allowed non-A2A Hermes external-agent declarations with HTTP JSON endpoints now register as approval-gated `external-agent`/`http-json` tools and post explicit prompt/metadata JSON bodies while preserving raw JSON responses. Normalized adapter manifests can now be exported/imported as portable JSON across CLI, TUI, remote daemon CLI, daemon HTTP, Tauri commands, and app controls, and manifest imports are forced back into quarantine before storage. A shared adapter doctor report now summarizes allowed/quarantined packages, ready/executable/metadata-only/unsupported capabilities, installable OpenClaw/AgentSkills skill entries, secret requirements, and high-risk findings across CLI, remote daemon CLI, daemon HTTP, Tauri commands, app controls, and TUI `/adapters doctor`, and the app doctor card surfaces installable skill counts separately from executable runtime readiness. Broader OpenClaw runtime execution compatibility remains incomplete end to end. |
| Run lifecycle hooks | Partial | `RunLifecycleHook`, `RunHookHandler`, `HookTrigger`, `HookFired`/`HookFailed`, trace summaries, CLI/TUI/daemon/Tauri render paths, `AdapterRegistry::lifecycle_hooks` | Observe-only hook registration and trace events exist for key lifecycle points. Allowed adapter hook declarations are loaded into CLI/TUI/daemon/Tauri harnesses, direct command handlers can run with JSON stdin, bounded timeouts, shell-interpreter rejection, bounded retry attempts from adapter hook metadata, and explicit `HookFailed` trace events with attempt/retry metadata. `before_context_built` handlers can return capped, secret-checked context fragments that the harness validates before merging. `before_tool_call` handlers can return bounded, secret-checked tool input mutations or explicitly deny a call before approval/execution. `tool_output_ready` handlers can also return bounded, secret-checked visible tool output mutations before `tool_completed` is recorded or returned. Trace-derived hook remediation plans are exposed through CLI/daemon/app paths, the app Trace panel can load the original prompt, replay it, replay it once with lifecycle hooks disabled, and exposes the same one-run hook override through `/replay --no-hooks`; it can also refresh persisted hook policy and persistently disable or re-enable failing hooks with confirmation. CLI/TUI hook-policy workflows can review failures, list disabled hooks, list all adapter-declared hooks with trigger/provenance/disabled-source metadata, require confirmation, and persist per-hook disables in active-profile policy, with agent-scoped disables available through CLI `--agent`, TUI `/hooks ... --agent`, daemon/Tauri policy calls, and app Disable Profile/Disable Agent controls. App run/save controls can now add exact lifecycle hook ids to skip for the next daemon/Tauri run and persist/reapply `disabled_lifecycle_hooks` on saved agent configs, while preserving the existing all-hooks one-run replay override. Hook policy responses now include effective source plus global/profile/agent lists so CLI/TUI/app surfaces can explain shadowed profile/global disables when an agent policy overrides them. Persisted hook-policy filtering is honored across CLI/daemon/Tauri hook loading, and hook catalog browsing is available through CLI `hooks available`, TUI `/hooks available`, daemon `/hooks/available`, Tauri `hook_available`, app `/hooks`, `/hooks available`, `/hooks list`, `/hooks review [run-id]`, `/hooks disable <hook-id> [--agent] --confirm`, and `/hooks enable <hook-id> [--agent] --confirm` shortcuts, and the app Trace panel Hook Catalog. |
| Voice, mobile, messaging | Partial | `agent-core::VoiceConfig`, layered `voice` config in `crates/agent-config`, `voice_transcribe`/`voice_speak` in `crates/agent-tools`, daemon `/bridges/telegram/webhook`, `/bridges/slack/slash`, `/bridges/teams/activity`, `/bridges/whatsapp/webhook`, and `/bridges/webhook` | Global/profile/agent config can persist and resolve voice input/output enablement, local/cloud backend choice, STT/TTS provider/model, voice, and tone with explain-config provenance and backend validation. The app saved-agent controls can now persist and reapply those per-agent voice settings instead of relying on hand-edited agent config. Resolved voice settings register approval-gated native voice tools across CLI/daemon/Tauri surfaces: local backends execute configured STT/TTS commands, and cloud speech uses an OpenAI-compatible audio API path with generated audio artifacts. The daemon also exposes Telegram, Slack, Teams activity, WhatsApp Cloud-style webhook, and generic JSON webhook bridges that route inbound messages through the same agent run/trace path, with optional configured Telegram secret-token, Slack signing-secret, Teams/WhatsApp/webhook bearer-token verification, plus optional outbound Telegram Bot API and `response_url` delivery with bounded retries. Failed outbound bridge deliveries are persisted as dead letters, listable with redacted URLs, manually retryable through daemon endpoints, remote CLI `bridge-deliveries` commands, and the app daemon-transport Adapters/Storage panel plus `/bridge-deliveries` slash shortcuts, and retryable by an opt-in daemon background worker. The app Artifacts panel plus app `/voice status`, `/voice capture`, `/voice stop`, `/voice transcribe [path]`, `/voice speak [text]`, and `/voice stage [text]` shortcuts now support microphone capture, inline playback of captured audio, scoped audio artifact saving, staging `voice_transcribe` with the saved `audio_path`, creating `voice_speak` output from composer/latest assistant text or explicit text, staging TTS calls, and playing generated audio artifacts inline through scoped data-url previews across Tauri and daemon transports. The frontend now exposes an installable mobile web shell with a web manifest and service worker. Native app-store/mobile binary packaging remains missing. |
| Payments and external code execution | Partial | Tool permissions include wallet/payment/shell categories and approval gates; `ShellTool`, `code_python`, `code_typescript`, configured shell/code sandbox wrappers, env-enabled x402 tools in `crates/agent-tools`, and opt-in x402 gating for daemon bridge endpoints | Shell, Python, and TypeScript snippets can run as approval-gated native tools with timeouts, captured stdout/stderr, output truncation, minimal inherited environment, temporary source directories for code, optional configured or auto-detected sandbox wrapper execution, fail-closed required wrapper mode, CLI direct-call support, app `/python <code>`, `/typescript <code>`, and `/ts <code>` shortcuts, app `/x402 request <url>`/`/payment x402-request <url>` shortcuts for x402 payment probes plus `/x402 required ...`/`/payment x402-required ...` and `/x402 settle ...`/`/payment x402-settle ...` shortcuts for receive-side x402 challenges and settlements, and daemon/Tauri direct-tool registry enablement. x402 payment tools can be enabled with `AGENT_PAYMENT_TOOLS`: `payment_x402_request` probes `PAYMENT-REQUIRED`, enforces explicit per-call or env spend limits before retry, and retries only with a supplied/configured `PAYMENT-SIGNATURE` or a redacted managed secret reference (`payment_signature_secret` / `AGENT_X402_SIGNATURE_SECRET`); retry output reports only the wallet signature source, not the signature. `payment_x402_required` builds receive-side 402 challenges; and `payment_x402_settle` verifies/settles incoming payment payloads through a configured facilitator before emitting a `PAYMENT-RESPONSE`. Telegram, Slack, Teams, WhatsApp, and generic daemon bridge endpoints can be protected with platform-specific or shared `X402_ACCEPTS` requirements, return HTTP 402 plus `PAYMENT-REQUIRED`, verify/settle incoming `PAYMENT-SIGNATURE` through a configured facilitator, and emit `PAYMENT-RESPONSE` before serving the agent response. `AGENT_DAEMON_X402_ACCEPTS` can now independently protect daemon execution endpoints (`/run`, `/run/start`, `/resume`, `/resume/start`, `/batch`, `/batch/resume`, and `/tool/...`) while leaving health/inspection endpoints open; `AGENT_DAEMON_X402_PATHS` can override that protected route set with exact paths or `/prefix/*` patterns. Broader wallet backends and non-x402 paid middleware remain incomplete. |
| Packaging and cross-platform behavior | Partial | Workspace crates, Tauri config, CI workflow, release packaging workflow, mobile packaging workflow, `packaging/release-artifacts.json`, `scripts/build-release-artifact.sh`, `scripts/verify-packaging.sh`, `scripts/verify-release-binaries.sh`, `scripts/verify-release-artifacts.sh`, `scripts/verify-mobile-packaging.sh`, `scripts/tauri-local.mjs` | Cross-platform code paths exist, Tauri bundling metadata is active with the app icon, and CI now validates cargo fmt, workspace clippy with warnings denied, the Windows/macOS/Linux matrix with frontend production builds, project-local Tauri CLI availability, packaging metadata checks, mobile web manifest/service-worker checks, native mobile package metadata checks, release binary builds for the CLI, daemon, and Tauri app, plus release binary artifact existence/size/executable checks. A versioned release-artifact manifest now declares the expected Linux/macOS/Windows installer outputs, Android/iOS mobile package outputs, updater signing outputs, and required signing/notarization environment gates using the frontend-local `@tauri-apps/cli` through a project-root wrapper so the Tauri config is discovered correctly. A manual/tagged release packaging workflow can run manifest-selected Linux/macOS/Windows Tauri bundle commands, validates required signing/updater env before building, imports the Apple signing certificate on macOS, uploads generated bundle artifacts, and reuses strict artifact verification after real builds. Strict release-artifact verification now accepts directory artifacts such as macOS `.app` bundles and rejects empty matched files/directories. A manual mobile packaging workflow installs Android/iOS prerequisites, initializes Tauri mobile projects, runs platform-scoped strict mobile verification, and can optionally build/upload signed Android/iOS artifacts through the shared release-artifact script when mobile signing/updater secrets are configured. The mobile verifier can check Android and iOS strict prerequisites independently, now rejects empty generated Android/iOS project directories without duplicating the empty-project error when a generated project is missing, and README documents the platform-specific `tauri -- android init` / `tauri -- ios init` plus strict verification commands. Local strict probes still fail without Android SDK/NDK, generated projects, and iOS CocoaPods/signing. The debug Tauri binary now exits with a concrete run/build reason instead of opening a blank WebView when Vite is unavailable and the built frontend is missing, incomplete, or stale. The frontend toolchain is on the patched Vite 6 line with clean npm audit output, and production builds emit relative asset URLs for Tauri custom-protocol/package loading. Live native mobile package production remains environment-dependent and not locally completed. |

## Next Recommended Slices

1. Native mobile packaging generation: run the mobile-packaging workflow for Android and iOS, then commit/review generated mobile project outputs if they should become source-controlled.
2. Packaging/cross-platform artifacts: run the release-packaging workflow with real signing/notarization secrets and verify uploaded Linux/macOS/Windows bundles.
3. Native package QA: run `scripts/verify-release-artifacts.sh --strict` against generated installer/mobile artifacts once signed package artifacts exist locally or in CI.
